@model MigrationService.Models.Language

@{
    ViewData["Title"] = "Удаление языка";
}

<div class="container mt-4">
    <div class="card">
        <div class="card-header">
            <h2>Удаление языка</h2>
        </div>
        <div class="card-body">
            <h3>Вы уверены, что хотите удалить этот язык?</h3>
            <div>
                <dl class="row">
                    <dt class="col-sm-2">@Html.DisplayNameFor(model => model.LanguageName)</dt>
                    <dd class="col-sm-10">@Html.DisplayFor(model => model.LanguageName)</dd>
                </dl>
                <form asp-action="Delete">
                    <input type="hidden" asp-for="LanguageID" />
                    <button type="submit" class="btn btn-danger">
                        <i class="bi bi-trash"></i> Удалить
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="bi bi-arrow-left"></i> Назад к списку
                    </a>
                </form>
            </div>
        </div>
    </div>
</div> 